= form_for @cat_dohod do |f|
  - if @cat_dohod.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cat_dohod.errors.count, "error")} prohibited this cat_dohod from being saved:"
      %ul
        - @cat_dohod.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
