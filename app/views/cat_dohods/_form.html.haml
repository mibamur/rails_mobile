= form_for @cat_dohod, "data-ajax" => false do |f|
  - if @cat_dohod.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cat_dohod.errors.count, "error")} prohibited this rab from being saved:"
      %ul
        - @cat_dohod.errors.full_messages.each do |msg|
          %li= msg
  %div
    .field
      = f.label t(:name)
      = f.text_field :name, :required=>""
    .field
      = f.label "По умолчанию"
      = f.check_box :odin, "data-role"=>"flipswitch"


  - 'TABB'
  %div
    .field
      = f.submit t('.save', :default => t("helpers.links.save")), :class => "btn btn-primary", "data-ajax" => false

