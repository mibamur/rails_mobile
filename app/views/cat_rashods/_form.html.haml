= form_for @cat_rashod do |f|
  - if @cat_rashod.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cat_rashod.errors.count, "error")} prohibited this cat_rashod from being saved:"
      %ul
        - @cat_rashod.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
