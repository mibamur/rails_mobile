= form_for @cat_rashod, "data-ajax" => false do |f|
  - if @cat_rashod.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cat_rashod.errors.count, "error")} prohibited this rab from being saved:"
      %ul
        - @cat_rashod.errors.full_messages.each do |msg|
          %li= msg
  %div{ :class=>"name"}
    .field
      = f.label t(:name)
      = f.text_field :name, :required=>""
    .field
      = f.label "По умолчанию"
      = f.check_box :odin

      - 'TABB'
  %div
    .field
      = f.submit t('.save', :default => t("helpers.links.save")), :class => "btn btn-primary", "data-ajax" => false

