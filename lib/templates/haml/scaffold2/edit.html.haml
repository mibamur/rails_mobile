.row
  .col-lg-12
    .panel.panel-default
      .panel-heading
        = link_to <%= index_helper %>_path, :class => 'btn btn-default' do
          %span.glyphicon.glyphicon-list-alt
            = t('.back', :default => t("helpers.links.back"))
      .panel-body
        = render '<%= plural_table_name %>/forms/form', modal: false
